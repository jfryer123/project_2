<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <% include ./partials/head.ejs %>
  </head>
  <body>
      <div class="nav">

        <h1>
          <% if(currentUser) { %>
            Welcome <%=currentUser.username%>!  </h1>
            <form action="/sessions?_method=DELETE" method="POST">
              <input type="submit" value="Logout" class="createButton4"/>
            </form>
            <a class="createButton4" href="/app">Create a tournament</a>

          </form>
      <% } else { %>

  <div class="nav">
  <h1>Welcome</h1>
  <div class="container">
  <div class="createButton">
  <a href="/users/new">Sign Up</a>
  </div>
  <div class="createButton">
  <a href="/sessions/new">Log In</a>
</div>
</div>
  <% } %>
  </div>
</div>

    </div>
  </body>
</html>
